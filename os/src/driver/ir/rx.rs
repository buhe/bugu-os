
// pub fn init(pin: usize,uart: UART2) {
//     let rev = IRrev::new(pin, uart);
//     rev.io_init();
// }

pub struct IRrev {
    // pin: usize,
// uart: UART2,
}

impl IRrev {
    pub fn new() -> Self {
        Self {}
    }
}
